apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-customer-service
  namespace: techchallenge
  labels:
    app: customer-service
spec:
  selector:
    app: customer-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP