apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-customer-service # ALTERAR
  namespace: techchallenge
  labels:
    app: tech-challenge-customer-service # ALTERAR
spec:
  selector:
    app: tech-challenge-customer-service # ALTERAR
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP